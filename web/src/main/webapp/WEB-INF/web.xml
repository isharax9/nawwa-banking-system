<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <!-- Faces Servlet Configuration -->
    <servlet>
        <servlet-name>Faces Servlet</servlet-name>
        <servlet-class>jakarta.faces.webapp.FacesServlet</servlet-class>
        <load-on-startup>1</load-on-startup>
    </servlet>

    <!-- Faces Servlet Mapping -->
    <servlet-mapping>
        <servlet-name>Faces Servlet</servlet-name>
        <url-pattern>*.xhtml</url-pattern>
    </servlet-mapping>

    <!-- Welcome File -->
    <welcome-file-list>
        <welcome-file>pages/login.xhtml</welcome-file>
        <!-- You might also have an index.html that redirects to login.xhtml -->
    </welcome-file-list>

    <!-- Error Pages (Optional but Recommended) -->
    <error-page>
        <exception-type>jakarta.faces.application.ViewExpiredException</exception-type>
        <location>/pages/login.xhtml</location>
    </error-page>
    <error-page>
        <error-code>404</error-code>
        <location>/pages/error/404.xhtml</location>
    </error-page>
    <error-page>
        <error-code>500</error-code>
        <location>/pages/error/500.xhtml</location>
    </error-page>

    <!-- Security Constraints (Optional, if not using filters for all security) -->
    <!-- Your AuthenticationFilter handles /pages/* redirect -->

    <!-- Session Timeout (in minutes) -->
    <session-config>
        <session-timeout>30</session-timeout>
        <tracking-mode>COOKIE</tracking-mode>
    </session-config>

    <!-- CDI Integration for JSF (automatic with Jakarta EE 8+, no need for beans.xml unless specific config) -->
    <!-- You might also need a META-INF/beans.xml in your WAR for CDI to be active -->
    <!-- src/main/webapp/META-INF/beans.xml (empty file) -->
</web-app>