<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <!-- Welcome file: When someone accesses your context root (e.g., http://localhost:8080/banking-web/),
         it will automatically forward to index.jsp -->
    <welcome-file-list>
        <welcome-file>index.jsp</welcome-file>
    </welcome-file-list>

    <!--
        Servlet Declarations and Mappings will go here.
        We'll add our LoginServlet here in the next step.
    -->

    <!--
        JSP Configuration (Optional, but can be useful)
        You can configure default JSP pages, or specific JSP properties.
    -->
    <jsp-config>
        <jsp-property-group>
            <url-pattern>*.jsp</url-pattern>
            <page-encoding>UTF-8</page-encoding>
            <!-- To enable EL in template text, useful for HTML content -->
            <scripting-invalid>true</scripting-invalid>
            <trim-directive-whitespaces>true</trim-directive-whitespaces>
        </jsp-property-group>
    </jsp-config>

    <!-- Error Pages (Optional, but good for user experience) -->


</web-app>